import SectionTitle from "../SectionTitle";
import VideoForm from "./VideoForm";

function VideoSection() {
  return (
    <section>
      <SectionTitle title="Upload Your Video" />
      <VideoForm />
    </section>
  );
}

export default VideoSection;
